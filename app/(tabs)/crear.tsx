import { Text } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';

export default function Crear() {
  return (
    <SafeAreaView>
        <Text className='color-blue-400'>Crear</Text>
    </SafeAreaView>
  );
}